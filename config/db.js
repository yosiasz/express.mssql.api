var config = {};

var config = {
		user: 'your.user.name',
        password: 'your.password',
        server: 'localhost',
        database: 'your.database',
        requestTimeout : 60000
    }

module.exports = config;    